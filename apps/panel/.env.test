# Test ortamı.
#
# `SESSION_DRIVER=memory` ŞART: api test istemcisi oturuma sunucu tarafından
# yazıyor (`withSession`) ve cookie sürücüsünde bu mümkün değil — CSRF gizi
# yerine ulaşmadığı için her form testi Shield'e takılıyordu.
NODE_ENV=test
SESSION_DRIVER=memory
ASSETS_DRIVER=fake
