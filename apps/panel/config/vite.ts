import { defineConfig } from '@adonisjs/vite'

const viteBackendConfig = defineConfig({
  /**
   * The output of vite will be written inside this
   * directory. The path should be relative from
   * the application root.
   */
  // `public/build`, `public/assets` DEĞİL: landing sayfasının görselleri
  // `/assets/` altında ve ikisi aynı yeri kullanırsa biri ötekini ezerdi.
  buildDirectory: 'public/build',

  /**
   * The path to the manifest file generated by the
   * "vite build" command.
   */
  // `manifestFile` BİLEREK yok: `defineConfig` onu `buildDirectory`'den
  // türetiyor. Kurulum onu ayrıca sabitliyordu ve `buildDirectory`'yi
  // değiştirmek yetmedi — çalışma anı hâlâ eski yola bakıyordu. Tek kaynak.

  /**
   * Feel free to change the value of the "assetsUrl" to
   * point to a CDN in production.
   */
  assetsUrl: '/build',

  scriptAttributes: {
    defer: true,
  },
})

export default viteBackendConfig
